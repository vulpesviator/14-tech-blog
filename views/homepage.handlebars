<!-- Blog Posts -->
<article class="row mb-5 justify-content-center">
    <div class="col-12 mx-auto">
        {{#each posts as |post|}}
            {{> full-post post}}
        {{/each}}
    </div>
</article>